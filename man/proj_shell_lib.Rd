% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shell.R
\name{proj_shell_lib}
\alias{proj_shell_lib}
\title{Path to the shell-side qproj path helpers}
\usage{
proj_shell_lib(mustWork = TRUE)
}
\arguments{
\item{mustWork}{If \code{TRUE} (default), abort when qproj is not installed with the
script present. If \code{FALSE}, return \code{""} the way \code{\link[base:system.file]{base::system.file()}} does.}
}
\value{
A single string: the absolute path to \code{qproj.sh}, or \code{""} when
\code{mustWork = FALSE} and it cannot be found.
}
\description{
Returns the filesystem path of \code{qproj.sh}, the Bash counterpart of the R path
discipline (\code{\link[=proj_path_target]{proj_path_target()}} / \code{\link[=proj_path_source]{proj_path_source()}}). A project driver
(\verb{analyses/p<code>.slurm}) sources it to get \code{path_target}, \code{path_source},
\code{path_raw}, \code{path_resource}, \code{path_run_state}, \code{create_dir_target} and
\code{qproj_nf_prepare}.
}
\details{
This is the authoritative answer for an \emph{installed} qproj. Drivers should not call
it directly in a hot loop -- they use the resolver emitted by
\code{\link[=proj_shell_bootstrap]{proj_shell_bootstrap()}}, which consults this only as one of several candidates
(see that function for why an installed package cannot be the sole mechanism).
}
\examples{
  \dontrun{
  proj_shell_lib()
  }
}
