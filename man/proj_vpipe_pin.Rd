% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vpipe_lock.R
\name{proj_vpipe_pin}
\alias{proj_vpipe_pin}
\title{Pin this project to an immutable vpipe release}
\usage{
proj_vpipe_pin(
  path_proj = "analyses/vpipe.lock",
  ref = "HEAD",
  requires = NULL,
  vpipe_repo = NULL,
  digest = FALSE
)
}
\arguments{
\item{path_proj}{Where to write the lock, relative to the project root.}

\item{ref}{Commit-ish in the vpipe checkout to pin. Defaults to the current release
tag if there is one; pass \code{"HEAD"} to pin whatever is checked out.}

\item{requires}{Compatibility range recorded in the lock. Defaults to
\code{">=<major>.<minor>,<major+1>.0"} for the resolved version.}

\item{vpipe_repo}{The vpipe source checkout to archive from. Defaults to
\verb{$VPIPE_HOME}, else \verb{~/vpipe}.}

\item{digest}{Passed through to the bill of materials: sha256 every container image
instead of fingerprinting by path, size and mtime. Slow (images are gigabyte-scale).}
}
\value{
Invisibly, the absolute path to the lock that was written.
}
\description{
Materialises the vpipe commit named by \code{ref} into the release store (on a host that can
write to it), then records the resulting release in \code{analyses/vpipe.lock} so every
driver in this project resolves to exactly that tree.
}
\details{
The digests are read back out of the materialised release's own manifest rather than
cut separately. A separately-cut bill of materials would describe the working checkout,
not the tree that will actually run, and the two can differ.
}
\examples{
  \dontrun{
  proj_vpipe_pin()                  # pin the current vpipe release
  proj_vpipe_pin(ref = "v0.9.0")    # pin a specific one
  }
}
