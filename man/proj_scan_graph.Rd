% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_graph.R
\name{proj_scan_graph}
\alias{proj_scan_graph}
\title{Scan a qproj workflow into a static dependency graph}
\usage{
proj_scan_graph(
  path = ".",
  workflow = "analyses",
  r_dir = "R",
  output_dir = ".qproj/graph",
  scan = c("step", "R", "db"),
  formats = c("json", "md", "html"),
  include_data_nodes = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{\code{character} project root (default: current directory).}

\item{workflow}{\code{character} workflow subdirectory under \code{path}
(default: \code{"analyses"}).}

\item{r_dir}{\code{character} directory of project R functions, relative to
\code{path} (default: \code{"R"}). If absent, the R layer is skipped silently.}

\item{output_dir}{\code{character} output directory, relative to \code{path}
(default: \code{".qproj/graph"}).}

\item{scan}{\code{character} subset of \code{c("step", "R", "db")} selecting
which detection layers to run. Default: all three on. Set
\code{scan = "step"} to reproduce v1 behavior.}

\item{formats}{\code{character} subset of \code{c("json", "md", "html")}. Choose
only the artifacts you need; \code{"html"} requires \code{visNetwork} and
\code{htmlwidgets}.}

\item{include_data_nodes}{\code{logical} if \code{TRUE}, also emit data-file nodes
in addition to step nodes. Currently a placeholder for future use.}

\item{quiet}{\code{logical} suppress CLI progress messages.}
}
\value{
Invisibly, the in-memory graph object (a list with \code{metadata},
\code{nodes}, \code{edges}, \code{warnings}, \code{render_order}).
}
\description{
Advanced feature for AI-assisted workflows. Statically scans a qproj
analysis workflow (no R code is executed) and emits up to three artifacts
under \code{output_dir}:
\itemize{
\item \code{qproj-graph.json} — full machine-readable dependency graph
\item \code{QPROJ_GRAPH_REPORT.md} — compact navigation report (designed for AI)
\item \code{qproj-graph.html} — self-contained interactive \code{visNetwork} view
}

The scanner detects three layers (each can be turned off via \code{scan}):
\itemize{
\item \strong{step layer} — always on. \code{path_source("<step>", ...)},
\code{path_target("<file>", ...)}, \code{here::here(path_raw, "d<step>-...", ...)},
\code{library()} / \code{pkg::fn}, \code{path_data} / \code{path_resource} / \code{path_raw}.
\item \strong{R layer} — calls into functions defined in the project's \verb{R/*.R}
(loaded via \code{devtools::load_all()} in each \code{.qmd}'s setup chunk).
Captures literal scalar arguments. Functions defined but never called
are flagged \code{unused = TRUE}.
\item \strong{db layer} — DBI table dependencies. \code{dbReadTable / dbWriteTable / dplyr::tbl} literal table args (EXTRACTED), and table names extracted
from SQL strings passed to \code{dbGetQuery / dbExecute} via regex
(INFERRED). \code{dbConnect()} paths are parsed (path_target / path_source /
here::here / file.path / literal) so each table node carries its
project-relative \code{db_file}.
}

Scanner output is consumed by AI assistants (Claude Code, Gemini CLI, etc.)
so they can reason about workflow structure without reading every \code{.qmd}.
Typical token saving on a 5-step workflow: ~10x vs. reading the raw files.
}
\section{Suggested packages}{

Calling \code{proj_scan_graph()} requires (Suggests, not Imports):
\code{knitr}, \code{rmarkdown}, \code{jsonlite}, plus \code{visNetwork} and \code{htmlwidgets}
if \code{"html"} is in \code{formats}.
}

\examples{
\dontrun{
  # From inside a qproj project root:
  qproj::proj_scan_graph()

  # JSON + MD only, skipping the HTML viewer:
  qproj::proj_scan_graph(formats = c("json", "md"))
}
}
