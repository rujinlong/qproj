% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shell.R
\name{proj_shell_bootstrap}
\alias{proj_shell_bootstrap}
\title{Canonical Bash bootstrap block for a project driver}
\usage{
proj_shell_bootstrap(step = NULL, optional = FALSE)
}
\arguments{
\item{step}{Optional step id. When supplied, the returned text includes the
\verb{_qproj_bootstrap --step <step>} call line and the surrounding failure handling;
otherwise only the function definition is returned and the caller writes its own
call. Must match \verb{^[A-Za-z0-9][A-Za-z0-9._-]*$} --- \strong{stricter than the shell-side
\verb{_qproj_validate_step}}, because this text is pasted into generated Bash, where
whitespace or a shell metacharacter would change the emitted script rather than
just name an odd directory. The value is single-quoted in the output as well.}

\item{optional}{If \code{TRUE}, the emitted call line tolerates a miss (warn and carry on,
with \verb{path_*} unavailable) instead of exiting. Only meaningful with \code{step}.}
}
\value{
A single string containing the Bash block, newline separated.
}
\description{
Returns the block of Bash a qproj project driver embeds to locate, source and
initialise the shell path helpers (\code{\link[=proj_shell_lib]{proj_shell_lib()}}). This function is the single
source of truth for that resolver: every driver carries a verbatim copy, because none
of the usual indirections survive the deployment targets --- \code{sbatch} runs the script
from a spool copy (so \verb{$0} / \code{BASH_SOURCE} are useless), PATH lookup reintroduces the
mixed-runtime hazard, and an R library is not guaranteed to be shared across compute
nodes (measured: \verb{~/R/...} is node-local on the spark cluster while \verb{~/github} is not).
}
\details{
Resolution precedence, in order:
\enumerate{
\item \verb{$QPROJ_SH} --- explicit override. \strong{Set but unreadable is a hard error}, never a
silent fallback: a typo must not quietly resolve to a different qproj.sh.
\item The installed package, via \code{\link[=proj_shell_lib]{proj_shell_lib()}} / \code{system.file()}.
\item \verb{$QPROJ_HOME/inst/scripts/qproj.sh} --- dev checkout, defaulting to
\verb{~/github/rujinlong/qproj}.
\item Otherwise fail, printing every location that was tried.
}

The block defines one function, \verb{_qproj_bootstrap}, which forwards its arguments to
\code{qproj_init} and exports the resolved \code{QPROJ_SH} so child scripts and nested \code{srun}
inherit it. A driver calls it with an explicit step:

\if{html}{\out{<div class="sourceCode">}}\preformatted{_qproj_bootstrap --step pc047e3 || exit 1
}\if{html}{\out{</div>}}

Pass the step explicitly rather than \code{--step-file}: under \code{sbatch} the basename would
be \code{slurm_script}.
}
\examples{
  cat(proj_shell_bootstrap(step = "010-import"))
}
