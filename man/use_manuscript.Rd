% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manuscript.R
\name{use_manuscript}
\alias{use_manuscript}
\title{Scaffold a Quarto-canonical manuscript}
\usage{
use_manuscript(
  name = "090-manuscript",
  path_proj = "analyses",
  open = rlang::is_interactive(),
  ignore = FALSE
)
}
\arguments{
\item{name}{Manuscript step name (no extension, no sub-directory). Manuscripts
conventionally sort last, e.g. \code{"090-manuscript"} or \code{"99-manuscript"}.}

\item{path_proj}{Project sub-directory to create the file in. Default
\code{"analyses"}.}

\item{open}{Whether to open the new file for editing. Defaults to interactive.}

\item{ignore}{Whether to add the created files to \code{.Rbuildignore}.}
}
\value{
Invisibly \code{NULL}, called for its side effects.
}
\description{
Create a dual-format (\code{pdf} + \code{docx}) manuscript \code{.qmd} from an opinionated
template and drop the companion \code{title.tex} LaTeX partial next to it. This
encodes the Quarto-canonical manuscript policy: one \code{.qmd} source renders to
\code{docx} (for supervisor Track Changes / journal submission), \code{pdf}
(self/preprint), and a \code{keep-tex} \code{.tex} escape hatch.
}
\details{
The \code{title.tex} partial renders a proper multi-author / multi-affiliation /
corresponding-author title block via \code{authblk} --- vanilla Quarto \code{pdf}
garbles author superscripts for anything beyond a single author. It is
referenced by \verb{template-partials:} in the manuscript YAML and is copied
verbatim (it uses pandoc \verb{$...$} template syntax, not whisker \code{{{ }}}).

After scaffolding, generate the Word \code{reference-doc} once from a styled
source document with \verb{vpipe docx extract-template <your-style.docx> -o manuscript-template.docx}.
}
\examples{
\dontrun{
  use_manuscript()                 # creates analyses/090-manuscript.qmd + title.tex
  use_manuscript("99-manuscript")
}
}
\seealso{
\code{\link[=use_qmd]{use_qmd()}} for ordinary analysis steps.
}
