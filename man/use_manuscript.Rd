% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manuscript.R
\name{use_manuscript}
\alias{use_manuscript}
\title{Scaffold a Quarto-canonical manuscript}
\usage{
use_manuscript(
  name = manuscript_default_name(),
  path_proj = "analyses/manuscript",
  open = rlang::is_interactive(),
  ignore = FALSE
)
}
\arguments{
\item{name}{Manuscript file stem (no extension, no sub-directory). Defaults to
\verb{<project-code>-manuscript}, where the project code is derived from the
enclosing project directory name (\code{p0101-BTEXvirome} -> \code{p0101},
\code{p0075e2-CRCprophage} -> \code{p0075e2}). When the directory name has no
code-shaped prefix (e.g. a generic project), it falls back to
\code{"090-manuscript"}. The stem is deliberately STABLE across revisions: date
and version stamps live only on the rendered products
(\verb{<stem>-vYYMMDD.N-submission.docx} etc.), injected by the
\code{manuscript-render} skill at render time --- so \code{here::i_am()},
\verb{_quarto.yml}, the \verb{data/<name>/} target, and git history stay anchored to
one unchanging filename.}

\item{path_proj}{Project sub-directory to create the file in. Default
\code{"analyses/manuscript"} (the qproj convention: every manuscript lives in
\verb{analyses/manuscript/}). The qmd's \code{here::i_am()} is anchored relative to the
\verb{analyses/} axis, so \verb{proj_path_*()} keep resolving to the shared
\verb{analyses/data/} tree regardless of the manuscript's sub-directory depth.}

\item{open}{Whether to open the new file for editing. Defaults to interactive.}

\item{ignore}{Whether to add the created files to \code{.Rbuildignore}.}
}
\value{
Invisibly \code{NULL}, called for its side effects.
}
\description{
Create a dual-format (\code{pdf} + \code{docx}) manuscript \code{.qmd} from an opinionated
template and drop the companion \code{title.tex} LaTeX partial next to it. This
encodes the Quarto-canonical manuscript policy: one \code{.qmd} source renders to
\code{docx} (for supervisor Track Changes / journal submission), \code{pdf}
(self/preprint), and a \code{keep-tex} \code{.tex} escape hatch.
}
\details{
The \code{title.tex} partial renders a proper multi-author / multi-affiliation /
corresponding-author title block via \code{authblk} --- vanilla Quarto \code{pdf}
garbles author superscripts for anything beyond a single author. It is
referenced by \verb{template-partials:} in the manuscript YAML and is copied
verbatim (it uses pandoc \verb{$...$} template syntax, not whisker \code{{{ }}}).

It also drops a minimal \verb{_quarto.yml} so the manuscript directory is its own
Quarto project root. Manuscripts live under \verb{analyses/manuscript/}, inside the
\verb{analyses/} Quarto project the analysis notebooks use; without this file Quarto
walks up to \verb{analyses/_quarto.yml} and merges that project's metadata into the
manuscript --- notably prepending its default \verb{author:} to the manuscript's own
author list (the rendered byline then survives only by Quarto's name
de-duplication, which is fragile across Quarto versions).

After scaffolding, generate the Word \code{reference-doc} once from a styled
source document with \verb{vpipe docx extract-template <your-style.docx> -o manuscript-template.docx}.
}
\examples{
\dontrun{
  # In p0101-BTEXvirome/: creates analyses/manuscript/p0101-manuscript.qmd
  use_manuscript()
  use_manuscript("99-manuscript")  # -> analyses/manuscript/99-manuscript.qmd
}
}
\seealso{
\code{\link[=use_qmd]{use_qmd()}} for ordinary analysis steps.
}
