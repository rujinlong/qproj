% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vpipe_lock.R
\name{proj_vpipe_check}
\alias{proj_vpipe_check}
\title{Verify this project's pin before starting any compute}
\usage{
proj_vpipe_check(path_proj = "analyses/vpipe.lock", strict = FALSE)
}
\arguments{
\item{path_proj}{The lock, relative to the project root (or an absolute path).}

\item{strict}{Treat environment drift as a failure. Off by default: the databases and
container images a run depends on are not pinnable at all yet (20 paths resolve
through \code{current} symlinks, and on this cluster 28 tool backends are unversioned
conda environments), so drift is expected and a check that is always red is a check
everyone learns to skip.}
}
\value{
Invisibly \code{TRUE}. Aborts if the contract is violated.
}
\description{
Runs \verb{vpipe contract check}, which is where the digest, version-range and API checks
live -- they need a real parser, so neither this function nor the Bash resolver
reimplements them.
}
\examples{
  \dontrun{
  proj_vpipe_check()
  }
}
